# CUA-Lark 产品需求文档（PRD）

> 状态：v0.2
> 最后更新：2026-04-26

## 1. 产品定位

CUA-Lark 是一个面向飞书桌面客户端的 GUI 智能测试系统。

它的目标不是依赖某个专用 “CUA 模型”，而是基于原始课题要求，采用以下通用技术路径：

1. 截图采集当前飞书桌面界面。
2. 用通用 OCR/VLM 理解界面状态和可交互元素。
3. 用系统级 GUI 操作执行鼠标和键盘动作。
4. 用截图语义判断和飞书 API 辅助验证结果。

也就是说，这个系统本质上是：

`Screenshot + OCR/VLM + GUI Executor + Verifier + Feishu API Oracle`

而不是：

`必须依赖专门的 CUA 模型`

## 2. 为什么不需要专门 CUA 模型

根据最原始的课题要求，视觉感知层本来就是：

- 截图 API
- UI 元素识别与定位
- VLM（GPT-4o / Claude 3.5 Sonnet / Qwen-VL / UI-TARS）

这说明赛题要求的是“系统能力闭环”，不是限定某个专门 CUA 模型。

对当前项目来说，更稳的工程路线是：

1. 先把截图、定位、基础动作、验证、trace 跑通。
2. 用通用 VLM 完成页面理解和单步动作决策。
3. 后续如果需要，再替换成更强的专用 GUI Agent / CUA 模型。

## 3. 产品目标

### 3.1 核心目标

1. 支持飞书桌面客户端基础 GUI 操作：
   - 单击
   - 双击
   - 右键
   - 拖拽
   - 滚动
   - 文本输入
   - 快捷键组合
2. 支持截图 + 通用 VLM 的单步 GUI 决策。
3. 支持多步串联执行。
4. 支持最小 trace 记录与报告。
5. 保留 `lark-cli` 作为环境准备和 API Oracle 能力。

### 3.2 非目标

以下内容不属于当前版本必须达成项：

1. 专用 CUA 模型接入
2. 完整 Recovery Agent
3. 复杂 OCR 训练或本地 OCR 模型部署
4. 完整的多产品 benchmark 平台

## 4. 产品架构

```text
用户指令
  -> 任务路由
     -> API 路线
        -> lark-cli / Feishu OpenAPI
     -> GUI 路线
        -> 窗口激活
        -> 窗口级截图
        -> OCR（可选）
        -> 通用 VLM 页面理解与单步决策
        -> PyAutoGUI 执行动作
        -> 截图验证
        -> API Oracle（可选）
        -> Trace / Report
```

## 5. 当前版本范围

### 5.1 已有

1. `lark-cli` API 路线
2. REPL 交互入口
3. GUI 基础动作执行器
4. 窗口级截图
5. 通用 VLM 单步决策 loop
6. trace 落盘

### 5.2 本次重点补齐

1. 文档口径从 “CUA 模型” 改为 “通用 VLM”
2. GUI 配置从 `CUA_*` 抽象为 `VLM_*` / `GUI_*`
3. 修复 Retina 截图像素与屏幕坐标映射
4. 增加坐标越界校验与重试

## 6. 关键设计原则

1. GUI 是主执行路径。
2. API 是辅助路径。
3. 不把系统绑死在某个专用模型上。
4. 优先窗口级截图，不做整屏瞎点。
5. 优先单步决策，避免长链误操作。
6. 先做稳的工程闭环，再做 fancy 的 Agent 能力。

## 7. 功能模块

### 7.1 视觉感知层

- 窗口激活
- 窗口 bounds 获取
- 窗口级截图
- OCR 预留接口
- VLM 页面理解

### 7.2 规划决策层

- 目标拆解
- 单步动作决策
- 坐标合法性检查
- 重试与纠错

### 7.3 执行操作层

- PyAutoGUI 鼠标操作
- PyAutoGUI 键盘操作
- 文本输入与中文粘贴兜底

### 7.4 状态验证层

- VLM 语义判断
- 截图对比
- API Oracle 预留

### 7.5 评估报告层

- JSON trace
- Markdown summary
- 操作截图归档

## 8. 版本路线

### M1

基础 GUI 动作可真实执行，窗口级截图可用。

### M2

通用 VLM 可基于窗口截图输出合法动作。

### M3

在 IM 子产品完成 “进群 -> 输入 -> 发送 -> 验证” 的端到端流程。

### M4

补齐 Docs / Calendar 的最小 GUI 用例。

## 9. 当前结论

这个项目不需要先找一个 “CUA 模型” 才能做。

当前更正确的工程定义应该是：

`飞书桌面 GUI 测试系统，底层由截图、通用 VLM、GUI 执行器和验证链路组成。`
