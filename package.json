{
  "name": "cua-lark-agent-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Electron shell for the Feishu GUI testing agent",
  "main": "desktop/main.js",
  "scripts": {
    "setup:python": "uv sync",
    "desktop": "node desktop/launch-electron.cjs",
    "desktop:dev": "node desktop/launch-electron.cjs"
  },
  "devDependencies": {
    "electron": "^41.3.0"
  }
}
